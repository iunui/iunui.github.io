{% if (page.categories) %}<ul class="list-unstyled">{% for cat in page.categories %}{% for post in site.categories[cat] limit:include.num %}<li><a href="{{site.baseurl}}{{post.url}}">{{post.title}}</a></li>{% endfor %}{% endfor %}</ul>{% endif %}
